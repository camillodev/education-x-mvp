# Protótipo Education X

O protótipo aprovado (source pixel-perfect + screenshots + chats de design) é a **fonte de verdade de layout e fluxo**.

Bundle completo em: `~/Documents/Claude/Projects/Impact X/Products/Education X/design-handoff/`
- `project/app/*.jsx` — source das 9 telas (referência pixel-perfect)
- `project/screenshots/*.png` — todas as telas
- `project/tokens.css` — tokens Alfabeto
- `chats/*.md` — intenção de produto

Regra: o protótipo manda no **layout e comportamento**; recriar com componentes reutilizáveis (não copiar o código inline do protótipo). Ver `docs/system-design.md` §3.
